{
  "name": "selenium-practice-portal",
  "version": "1.0.0",
  "description": "Selenium practice portal with backend login",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check main.js"
  },
  "engines": {
    "node": ">=18"
  }
}
